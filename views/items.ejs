<!DOCTYPE html>
<html>
  <head>
    <title>Items</title>
    <link rel='stylesheet' href='/stylesheets/style.css' />
  </head>
  <body>
    <h1>Items</h1>
    <table>
      <tr>
        <th>Name</th>
        <th>Description</th> 
      </tr>
      <% for (var i = 0; i < items.length; i++) { %>
        <tr>
          <td><%= items[i].name %></td>
          <td><%= items[i].description %></td> 
        </tr>
      <% } %>
    </table>
  </body>
</html>
