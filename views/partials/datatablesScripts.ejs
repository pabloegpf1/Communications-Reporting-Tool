<script>
    $.fn.dataTable.moment( 'D/M/YYYY' );
    $('table').DataTable( {
      paging: true,
      searching: true,
      ordering: true,
      "order": [],
      "columnDefs": [ {
          "targets": 'no-sort',
          "orderable": false,
        }
    ]
    } );
  </script>